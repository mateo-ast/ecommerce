<style>
  .hero {
    position: relative;
    height: 500px;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('https://images.unsplash.com/photo-1518770660439-4636190af475?w=1600&h=800&fit=crop') center/cover;
    opacity: 0.3;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 800px;
    padding: 0 20px;
  }

  .hero h1 {
    font-size: 3.5rem;
    font-weight: 800;
    color: #ffffff;
    margin-bottom: 1rem;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
  }

  .hero p {
    font-size: 1.3rem;
    color: #e0e0e0;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .hero-btn {
    display: inline-block;
    padding: 16px 40px;
    background: linear-gradient(135deg, #e94560 0%, #c81d4e 100%);
    color: #ffffff;
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 50px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    box-shadow: 0 4px 15px rgba(233, 69, 96, 0.4);
  }

  .hero-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(233, 69, 96, 0.6);
  }

  @media (max-width: 768px) {
    .hero {
      height: 400px;
    }

    .hero h1 {
      font-size: 2.2rem;
    }

    .hero p {
      font-size: 1rem;
    }
  }
</style>

<section class="hero">
  <div class="hero-content">
    <h1>Tecnología de Última Generación</h1>
    <p>Explora nuestro catálogo de laptops, smartphones, audífonos y más. Las mejores marcas al mejor precio.</p>
    <a href="#productos" class="hero-btn">Explorar Tecnología</a>
  </div>
</section>
